{
  "name": "tarek-notAffiliate",
  "private": true,
  "scripts": {
    "build": "npm run build:css && npm run build:bundle",
    "build:css": "tailwindcss -i ./src/tailwind-input.css -o ./css/tailwind.css --minify",
    "build:bundle": "node scripts/bundle-css.mjs",
    "build:og": "node scripts/build-og-image.mjs",
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.10",
    "sharp": "^0.34.5",
    "tailwindcss": "^3.4.17"
  }
}
